
  cloud:
    gateway:
      routes:
      - id: ORDER-SERVICE
        uri: lb://ORDER-SERVICE
        predicates:
        - Path=/api/order/**
      - id: ORDER-SERVICE
        uri: lb://ORDER-SERVICE
        predicates:
        - Path=/api/order/**         
server:
  port: 9191
management:
  endpoints:
    jmx:
      exposure:
        include: refresh,*
        
   
